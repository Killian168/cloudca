{
  "name": "cloudca",
  "version": "0.0.1",
  "description": "Cloud backend for club app built on AWS.",
  "author": "Killian O'Dalaigh",
  "devDependencies": {
    "serverless-iam-roles-per-function": "^3.2.0",
    "serverless-prune-plugin": "^2.0.1",
    "serverless-python-requirements": "^5.3.0"
  },
  "scripts": {
    "postinstall": "pip3 install -r requirements.txt",
    "test": "pytest -v test/"
  },
  "dependencies": {
    "serverless": "^3.0.0"
  }
}
